// cannot use override assignment because: An operator function must either be a member of a class or have at least one parameter of class type.

ptr[i] &= ~(0x01 << j);
ptr[i] |= (0x01 << j);   // Turn it on!

